> Values in [brackets] are read from `.claude/workflow-infra.md` and `.claude/workflow-smoke.md` at runtime.
> ⚠️ DRAFT — pre-implementation. Generated by `/spec-create` Step 7 from contract only.
> `/spec-implement` Step 5 refines this into the final smoke test based on what was actually built.

# Smoke Test (DRAFT) — {{spec-name}}
_Generated: {{date}}_
_Spec: specs/{{spec-name}}.md_
_Status: DRAFT — pre-implementation_

---

## Purpose

Acceptance criteria the implementation must satisfy. The agent treats this as its target while coding. The human reviews to confirm the contract's externally observable behavior matches expectation BEFORE any code is written.

---

## Prerequisites (from contract)

<!-- DB state or environment required by the contract. -->
<!-- Pre-impl: derived from spec assumptions, not from runtime knowledge. -->

- [prerequisite from spec]

---

## Steps (contract-derived)

<!-- One happy-path + one failure-path per external I/O described in the spec contract. -->
<!-- These are CONTRACT EXPECTATIONS, not yet runnable scripts — `/spec-implement` will fill in exact request shapes and selectors. -->

### S01 — {{short title}} [HTTP] [happy-path]

**Contract behavior:**
- Trigger: [from spec — e.g., POST /api/X with valid body]
- Expected outcome: [from spec — e.g., returns status: ok with rows_inserted > 0]

**Refinement needed at impl:** exact endpoint path, exact body shape, exact response field assertions.

---

### S02 — {{short title}} [HTTP] [failure-path]

**Contract behavior:**
- Trigger: [from spec failure path — e.g., malformed body]
- Expected outcome: [from spec — e.g., status: failed, error: parse_error, no DB writes]

**Refinement needed at impl:** exact error code, exact error message format.

---

### S03 — {{short title}} [DB] [happy-path]

<!-- Skip if [db_type] is none. -->

**Contract behavior:**
- After [trigger], [table.column] should reflect [expected state from spec].

**Refinement needed at impl:** exact migration columns, exact verification SQL.

---

### S04 — {{short title}} [[ui_driver]] [happy-path]

<!-- Skip if [ui_driver] is none. -->

**Contract behavior:**
- User does [action from spec], should see [expected outcome from spec].

**Refinement needed at impl:** exact selectors, exact wait conditions, exact assertion targets.

---

## Open questions for impl

Behaviors the contract did not fully pin down — `/spec-implement` should resolve these against the architecture/patterns and document in context update:

- [question]
