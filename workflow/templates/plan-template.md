> Values in [brackets] are read from `.claude/workflow-config.md` at runtime.
> This file is the implementation plan for a spec. Reviewed independently of the contract spec.
> Generated by `/spec-create` Step 7. Re-read by `/spec-implement` Step 1.

# Implementation Plan — {{spec-name}}
_Generated: {{date}}_
_Spec: specs/{{spec-name}}.md_

---

## Files to create or modify

```
[backend_dir]/path/to/file       — create — [what it contains]
[backend_dir]/path/to/other      — modify — [what changes]
[backend_dir]/tests/unit/...     — create — [what it tests]
```

Estimated total LOC delta: ~NNN.

---

## DEC alignment

For each DEC this plan touches, state how the plan honors it.

- **DEC-NNN — [title]**: honored by [file/approach]. No violation.
- **DEC-NNN — [title]**: extends — proposes new sub-rule [X]. Requires `decision-sync` after impl.

If any DEC would be contradicted: STOP. Resolve via supersession before this plan can be approved.

---

## Components consumed (reuse mapping)

From `docs/COMPONENT-LIBRARY.md`:

- `[component-name]` — used in [file/screen]
- `[component-name]` — used in [file/screen]

Reuse ratio (screen specs): N consumed / M total visible elements = X.XX. Must be ≥ 0.6 for screen specs.

---

## New patterns introduced

If anything is not already in `docs/PATTERNS.md`:

- [pattern name] — [one-line description] — [where used]

Each new pattern must either be already documented OR accompanied by a post-implement proposal in the context update.

---

## Architecture-fit statement

One paragraph:

> Implements [layer] per ARCHITECTURE §[N]. Does not violate boundaries §10. Assumes DEC-[list]. New components: [list or "none"]. Touches modules: [list].

---

## Risks and mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| [risk] | low/med/high | low/med/high | [how addressed in plan] |

Minimum: list at least one risk per external I/O boundary the spec touches.

---

## Rollback plan

If this spec ships and we need to revert:

- [ ] Files to delete: [list]
- [ ] Files to revert: [list]
- [ ] DB migration to roll back (if any): [migration name]
- [ ] Config / env changes to undo: [list or "none"]
- [ ] Downstream specs blocked by revert: [list or "none"]

---

## Ambiguities resolved during discovery

What was unclear at spec-create time and how it was resolved by human input:

- [ambiguity] → [resolution] (per discovery dialogue)

---

## Open questions deferred to implementation

Questions that did not block planning but may surface during impl:

- [question] — fallback if ambiguous: [default behavior]
