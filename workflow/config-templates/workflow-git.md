# Workflow Git Config
# VCS and PR conventions — read by ship and spec-create.
# Populated by project-init-new during project initialization.

## Branching
branch_prefix:          # feat | feature | fix
main_branch:            # main | master

## Commits
# Comma-separated list of valid conventional commit scopes for this project
commit_scopes:          # e.g. backend,frontend,infra

## PR
pr_tool:                # gh | gitlab | bitbucket | none
